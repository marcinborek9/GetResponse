How to install:

1. download and install node.js from https://nodejs.org/en/
2. open windows powershell / git bash / bash
3. go to directory with all files (cd GetResponse)
3. npm install 
4. npm install --global gulp-cli
5. npm install --save-dev gulp-watch
6. gulp watch
7. open in browser localhost:3000
